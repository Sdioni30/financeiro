spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: chave-secreta-para-testes-minimo-32-caracteres!!
  expiration: 3600000

cors:
  allowed-origins: http://localhost

mercadopago:
  access-token: test-token-stub
  preco: 9.90
  return-url: http://localhost/
  webhook-url: http://localhost/api/pagamento/webhook
  webhook-secret: test-webhook-secret
  sandbox: true
  contas-gratuitas: sdioni30@gmail.com